# Copy this file to .streamlit/secrets.toml for local use,
# OR add these as secrets in the Streamlit Cloud dashboard (Settings → Secrets).

ANTHROPIC_API_KEY = "sk-ant-..."

# Optional: Polygon.io for richer data
# POLYGON_API_KEY = "..."
